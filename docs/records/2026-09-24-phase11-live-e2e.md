# Live E2E 联调记录 — Phase11（已脱敏）

> 真实 GitHub App 端到端首次成功证据。勿含密钥 / PEM / PAT / webhook secret / ADMIN_TOKEN。  
> 参考：[docs/e2e-demo.md](../e2e-demo.md) · `scripts/preflight_live.py` · [records README](./README.md)

- **日期**：2026-09-24（Asia/Shanghai）
- **操作者**：lljk8187-coder
- **仓库**：`lljk8187-coder/pr-sentinel`
- **PR Sentinel 版本 / commit**：release **1.6.0**（本记录随主线发布；触发 PR 分支 tip `4c8cca3640cda88acc5381a31f9f036f79e9269b`）

---

## 1. 环境

- [x] Compose / 本机进程已启动（api + worker + redis + postgres）
- [x] `USE_FIXTURES=false`
- [x] Auth：GitHub App（`GITHUB_APP_ID` + PEM PATH）——**勿写密钥**
- [x] `GITHUB_WEBHOOK_SECRET` 已配置且与 App Webhook Secret 一致
- [x] `WEBHOOK_SKIP_VERIFY=false`（HMAC on）
- [x] `ADMIN_TOKEN` 已配置

备注：Docker daemon 在联调机不可用；改用 **host Redis + Postgres + uvicorn/arq**。运行目录与密钥文件未入库（`/workspace/pr-sentinel-live/*`、`*.pem`、`.env` 等排除）。

## 2. App / Install

- [x] GitHub App 已创建
- [x] Repository permissions 含：**Checks Read & write**、Contents Read、Pull requests R/W、Issues R/W
- [x] 已 Install 到测试仓；权限已 Accept
- App 名称：`pr-sentinel-live-e2e`
- App ID：`5057274`
- installation id：`164325792`（repo-limited to `pr-sentinel`）

## 3. smee / Webhook

- [x] smee.io channel → `http://localhost:8000/webhooks/github`
- [x] App Webhook URL 指向 smee
- [x] 开 PR 后 API 收到 webhook（HMAC 校验通过，非 401）

Transport：smee.io → localhost:8000/webhooks/github（HMAC on）

## 4. Preflight 结果摘要

```bash
python scripts/preflight_live.py
```

- exit code：`0`
- 开 PR 前：**PASS**
- FAIL 数：`0`
- PASS 项含 live `USE_FIXTURES=false` + App 凭证形态检查

## 5. 触发与产物

- PR URL：https://github.com/lljk8187-coder/pr-sentinel/pull/1  
  （触发文件仅在 PR 分支 `live-e2e/phase11-*`；**未**合入 main；PR 可保持 open）
- job_id：`ff30359c-9465-4366-8c5e-66c53186a19d` — **status success**
- delivery_id：`5d6b47c0-b7ed-11f1-8caf-7209e77ae36a`
- sha：`4c8cca3640cda88acc5381a31f9f036f79e9269b`

| 检查项 | 出现？ | 备注 |
| --- | --- | --- |
| Postgres / 控制台 job success | [x] | job success；分析侧有预期 secrets hit |
| Check Run `pr-sentinel` | [x] | id `107544648831`；conclusion **failure**（预期：secrets 命中）；url https://github.com/lljk8187-coder/pr-sentinel/runs/107544648831 |
| Sticky summary comment | [x] | by `pr-sentinel-live-e2e[bot]` on PR#1 |
| Inline review comment | [x] | `examples/live_e2e_trigger/trigger_secrets.py` line 2 — secrets rule |
| Findings 含 source / rule_id | [x] | 1 finding：rules / secrets；LLM skipped（无 OpenAI key） |

## 6. 截图说明（勿贴密钥）

| 说明 | 文件名或外链 | 已脱敏？ |
| --- | --- | --- |
| Check Run | https://github.com/lljk8187-coder/pr-sentinel/runs/107544648831 | [x] |
| Sticky / inline | PR#1 评论与 review | [x] |
| 控制台 job | job_id 如上（本地 console，未入库截图） | [x] |

## 7. 失败排查（如有）

| 现象 | 已查 | 结论 |
| --- | --- | --- |
| Check Run conclusion failure | [x] | **预期**：触发文件故意命中 secrets 规则 |
| LLM 未跑 | [x] | 环境无 OpenAI key；规则路径单独证明 live 链路 |

## 8. 结论

- [x] 联调目标达成：真 App → smee → HMAC webhook → worker → Check Run + sticky + inline（rules secrets）全链路成功
- 仍为可选能力：他人复现不强制；fixtures smoke 仍是默认路径

签字 / 备注：Phase11 live e2e SUCCEEDED 2026-09-24 Asia/Shanghai；证据随 **1.6.0** 入库。
