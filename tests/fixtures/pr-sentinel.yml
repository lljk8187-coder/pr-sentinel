version: 1
summary_comment: true
check_run: true
inline_comments: true
update_strategy: update
ignore_paths:
  - "docs/**"
  - "**/*.md"
diff:
  max_pages: 5
  per_page: 100
  max_files: 300
rules:
  secrets:
    enabled: true
  large_files:
    enabled: true
    max_bytes: 500
    max_additions: 2000
  weakened_tests:
    enabled: true
  skipped_tests:
    enabled: true
  dangerous_commands:
    enabled: true
analyzer:
  mode: rules
